import ContactsUserCard from "./ContactUserCard"

function Contacts() {
    return Contacts (
        <header>
            <h4>who's gaming right now?</h4>
            <ul>
                <ContactsUserCard/>
            </ul>
        </header>
    )
}

export default Contacts